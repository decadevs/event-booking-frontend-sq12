import React from "react";
import { Button } from "@mui/material";

const PaginationButton = () => {
    return (
        <Button variant="contained" color="primary"> Load More Events</Button>
    )
}

export default PaginationButton